<Window x:Class="BatchProcessor.Scripts.BulkDownload.BulkDownloadAndProcessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Bulk Download and Process" Height="750" Width="900"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="200"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Back Button -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="15" Margin="0,0,0,15" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Back Button -->
                <Button Grid.Column="0" x:Name="BtnBack" Content="⬅" Width="40" Height="40" FontSize="20"
                        Background="Transparent" Foreground="White" BorderThickness="0"
                        Click="BtnBack_Click" Cursor="Hand" Margin="0,0,10,0"
                        ToolTip="Back to Mode Selection">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#34495E"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Title -->
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Bulk Download and Process" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock Text="Download DWG files and process them with a custom command" FontSize="12" Foreground="#ECF0F1" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Links File and Folders -->
        <GroupBox Grid.Row="1" Header="Download Links and Folders" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <!-- Links File -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Links File:" VerticalAlignment="Center" Margin="0,5,0,5" ToolTip="Text file with download URLs (one per line)"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtLinksFile" Margin="5" Padding="5" VerticalAlignment="Center"/>
                <Button Grid.Row="0" Grid.Column="2" Content="Browse..." Margin="5" Padding="5" Click="BtnBrowseLinksFile_Click"/>

                <!-- Download Folder -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Download Folder:" VerticalAlignment="Center" Margin="0,5,0,5"/>
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtDownloadFolder" Margin="5" Padding="5" VerticalAlignment="Center"/>
                <Button Grid.Row="1" Grid.Column="2" Content="Browse..." Margin="5" Padding="5" Click="BtnBrowseDownloadFolder_Click"/>

                <!-- Output Folder -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Output Folder:" VerticalAlignment="Center" Margin="0,5,0,5"/>
                <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtOutputFolder" Margin="5" Padding="5" VerticalAlignment="Center"/>
                <Button Grid.Row="2" Grid.Column="2" Content="Browse..." Margin="5" Padding="5" Click="BtnBrowseOutputFolder_Click"/>
            </Grid>
        </GroupBox>

        <!-- DLL Path -->
        <GroupBox Grid.Row="2" Header="📦 DLL Path" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="DLL Path:" VerticalAlignment="Center" Margin="0,5,0,5"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtDllPath" Margin="5" Padding="5" VerticalAlignment="Center"/>
                <Button Grid.Row="0" Grid.Column="2" Content="Browse..." Margin="5" Padding="5" Click="BtnBrowseDll_Click"/>
            </Grid>
        </GroupBox>

        <!-- AutoCAD Settings -->
        <GroupBox Grid.Row="3" Header="🔧 AutoCAD Settings" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="AutoCAD Path:" VerticalAlignment="Center" Margin="0,5,0,5"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtAutoCADPath" Margin="5" Padding="5" VerticalAlignment="Center"/>
                <Button Grid.Row="0" Grid.Column="2" Content="Browse..." Margin="5" Padding="5" Click="BtnBrowseAutoCAD_Click"/>
            </Grid>
        </GroupBox>

        <!-- Processing Settings -->
        <GroupBox Grid.Row="4" Header="⚙️ Processing Settings" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Command Name -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Command Name:" VerticalAlignment="Center" Margin="0,5,0,5"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtCommandName" Margin="5" Padding="5" VerticalAlignment="Center" Text="RunPreScrutinyValidationsBatch"/>

                <!-- Max Parallel Processes -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Max Parallel:" VerticalAlignment="Center" Margin="0,5,0,5"/>
                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="5">
                    <TextBox x:Name="TxtMaxParallel" Width="100" Padding="5" Text="4" VerticalAlignment="Center"/>
                    <TextBlock Text="processes" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="Gray"/>
                </StackPanel>

                <!-- Verbose Logging -->
                <CheckBox Grid.Row="2" Grid.Column="1" x:Name="ChkVerbose" Content="Enable verbose logging" Margin="5" VerticalAlignment="Center"/>
            </Grid>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
            <Button x:Name="BtnRun" Content="▶ Download and Process" Width="250" Height="40" FontSize="16" FontWeight="Bold"
                    Background="#E74C3C" Foreground="White" Click="BtnRun_Click" Margin="5"/>
        </StackPanel>

        <!-- Progress/Status -->
        <GroupBox Grid.Row="6" Header="📊 Status" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" x:Name="TxtStatus" Text="Ready to process..."
                           TextWrapping="Wrap"
                           FontWeight="SemiBold"
                           FontSize="12"
                           Padding="5"
                           MinHeight="30"/>
                <TextBlock Grid.Row="1" x:Name="TxtExecutionTime" Text=""
                           TextWrapping="Wrap"
                           FontWeight="Normal"
                           FontSize="11"
                           Foreground="#7F8C8D"
                           Padding="5,0,5,5"
                           Visibility="Collapsed"/>
            </Grid>
        </GroupBox>

        <!-- Log Output -->
        <GroupBox Grid.Row="7" Header="📝 Log Output" Margin="0,0,0,10" Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="TxtLog" IsReadOnly="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto" Background="#F8F9FA"
                         FontFamily="Consolas" FontSize="11" BorderThickness="0"/>
            </ScrollViewer>
        </GroupBox>

        <!-- Footer -->
        <Border Grid.Row="8" Background="#ECF0F1" Padding="10" CornerRadius="5">
            <TextBlock Text="💡 Tip: Create a text file with download URLs (one per line) before starting"
                       TextWrapping="Wrap" Foreground="#7F8C8D" FontSize="11" HorizontalAlignment="Center"/>
        </Border>
        </Grid>
    </ScrollViewer>
</Window>
